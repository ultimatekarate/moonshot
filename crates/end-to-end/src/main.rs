fn main() {
    // Stub: see docs/plan.md "End-to-end glue".
    // Real implementation runs the same trace through:
    //   (a) model::cv_2d (reference)
    //   (b) codegen_demo::Filter4x2 (macro-expanded)
    //   (c) target/qemu-trace.bin (if --with-qemu-trace)
    // and exits nonzero if max-abs-diff > 1e-4.
    println!("end-to-end stub — not yet implemented");
}
